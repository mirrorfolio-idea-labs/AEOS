{
  "name": "@aeos/cli",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "private": true,
  "bin": { "aeos": "./dist/main.js" },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@aeos/contracts": "workspace:*",
    "@aeos/sdk": "workspace:*"
  },
  "devDependencies": {
    "@aeos/api": "workspace:*",
    "@aeos/kernel": "workspace:*",
    "@aeos/provider-core": "workspace:*",
    "@types/node": "^22",
    "vitest": "^2.1.8"
  }
}
