{
  "name": "@aeos/aeosd",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/daemon.js",
  "bin": { "aeosd": "./dist/main.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "vitest run",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@aeos/contracts": "workspace:*",
    "@aeos/kernel": "workspace:*",
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22"
  }
}
