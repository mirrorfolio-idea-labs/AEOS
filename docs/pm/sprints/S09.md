# Sprint 09 — P2.M5 PTY attach + co-edit guard

- **Opened:** 2026-08-25 (overnight continuation, immediately after the
  P2.M4 close-out)
- **Goal:** P2.M5 exit gate — mid-session human takeover demonstrated
  end-to-end (interactive terminal over WS/xterm while the canonical stream
  stays coherent; release returns to headless), and a human worktree edit
  pauses the task behind an `approval.request` (ADR-009).
- **Milestone:** [P2.M5](../../ROADMAP.md#m5--pty-attach--co-edit-guard--)
  · Plan: `docs/superpowers/plans/2026-08-25-aeos-p2-m5-pty.md`
- **Status:** `[~]` open.

## PM tasks

#### PM-S09-1 `[x]` Author the P2.M5 milestone plan just-in-time.

## Log (append-only)

- 2026-08-25 — Sprint opened after Kabeer explicitly approved the milestone's
  new third-party dependencies (`node-pty`, `@xterm/xterm`,
  `@xterm/addon-fit`) via a direct question mid-session. One addition beyond
  the literal approval list: `@fastify/websocket` — first-party fastify
  plugin required by the approved T2 scope ("WebSocket attach endpoint");
  recorded here for supply-chain transparency. Design decisions live in the
  plan head: additive protocol messages (no version bump), PTY hosts an
  interactive shell (not the harness CLI), PTY bytes stay out of the
  canonical taxonomy with a metadata-only `pty.log`, release kills the
  shell, attach gated to `execute_commands: allow`, co-edit target in v0.2
  is the agent dir repo. No per-task cards (single-agent execution, S05–S08
  precedent).
