# Sprint 01 — Close M1, open M2

- **Opened:** 2026-07-13
- **Sprint goal:** M1 exit gate passes (all T-accepts green in CI on `main`) and the M2 plan exists.
- **Milestone:** [P1.M1](../../ROADMAP.md#m1--monorepo-scaffold--contracts-package--) · Plan: [M1 contracts plan](../../superpowers/plans/2026-07-13-aeos-p1-m1-contracts.md)
- **Branch in flight:** `feat/aeos-p1-m1-contracts` (T1–T5 committed, reviews clean — see `.superpowers/sdd/progress.md`)
- **Status:** `[~]`

## Scope

### Build tasks (status owned by ROADMAP)

| ID | Title | Card |
|---|---|---|
| AEOS-P1.M1.T6 | Boundary enforcement (dependency-cruiser) + CI pipeline | [card](../tasks/AEOS-P1.M1.T6.md) |

### PM tasks (status owned here)

#### PM-S01-1 `[ ]` Merge `feat/aeos-p1-m1-contracts` → `main`
- **Objective:** land M1 on `main` so the exit gate ("CI green on `main`") can be evaluated.
- **Depends on:** AEOS-P1.M1.T6 (CI must exist and be green on the branch first).
- **Owner:** Kabeer (merge approval) — see `superpowers:finishing-a-development-branch` for options.
- **Accept:** `main` contains T1–T6 commits; CI workflow runs green on `main` (or, if no GitHub remote CI is wired, the CI-identical local chain in the T6 card passes on `main` and this is noted here).
- **Priority:** P0

#### PM-S01-2 `[ ]` M1 exit-gate documentation pass
- **Objective:** clear the documentation debts deliberately deferred to M1 exit.
- **Context:** deferred items are recorded in `.superpowers/sdd/progress.md`.
- **Steps:** (1) reword ROADMAP M1.T4 accept text — implementation is a runtime golden-fixture test, not a compile-time exhaustiveness check (plan-approved deviation); (2) flip M1 milestone marker to `[x]` once T6 + PM-S01-1 are done; (3) regenerate `BOARD.md` + `TRACEABILITY.md`; (4) run the R5 drift scan from [README](../README.md); (5) archive completed task cards.
- **Depends on:** PM-S01-1.
- **Owner:** any agent. **Priority:** P1
- **Accept:** drift scan reports zero findings; ROADMAP T4 text matches reality.

#### PM-S01-3 `[x]` Deferred review minors (tests)
- **Objective:** close the review minors deferred during T1–T5 (source: `.superpowers/sdd/progress.md`).
- **Steps:** (1) add empty-string rejection tests for `.min(1)` fields (envelope/domain); (2) add a credential `.strict()` test that isolates the stray-key invariant (valid `secretRef` + extra `apiKey` key); (3) decide whether test files should be typechecked (tsconfig `include:["src"]`) and either include them or record the decision here.
- **Depends on:** nothing (can run on the feature branch now).
- **Owner:** any agent (good first delegation candidate). **Priority:** P2
- **Accept:** `pnpm test` green with the new cases; each deferred line in progress.md is either resolved or has a recorded decision.

#### PM-S01-4 `[ ]` Author M2 plan
- **Objective:** write `docs/superpowers/plans/<date>-aeos-p1-m2-kernel.md` covering M2.T1–T5 to the same cold-start standard as the M1 plan.
- **Context:** ROADMAP M2 context brief + spec §6–§7. **Gate:** may only start after M1 exit gate passes (ROADMAP rule).
- **Depends on:** PM-S01-2.
- **Owner:** planning agent + Kabeer approval. **Priority:** P1
- **Accept:** plan committed; ROADMAP M2 "Plan:" line points at it; Sprint 02 opened.

## Definition of done (sprint)

M1 marked `[x]` in ROADMAP with exit gate verifiably passed, drift scan clean,
M2 plan approved and linked. Closing this sprint = opening `S02.md`.

## Log (append-only)

- 2026-07-13 — **PM-S01-3 completed**: empty-string `.min(1)` rejection tests
  (envelope + domain), `.strict()` stray-key isolation test (valid secretRef +
  extra apiKey). tsconfig decision: tests/scripts ARE typechecked via new
  `tsconfig.test.json` (build tsconfig untouched per plan); this immediately
  caught a real gap — missing `@types/node` devDep, now added. Suite: 19/19
  green, typecheck covers src+test+scripts. All three deferred minors from
  `.superpowers/sdd/progress.md` resolved (T4 reword remains with PM-S01-2).

- 2026-07-13 — **AEOS-P1.M1.T6 completed** (resumed from `13035e9`): D6 fixed
  via `apps/.gitkeep`; RED/GREEN proven (scratch violation → depcruise error →
  clean pass); `.github/workflows/ci.yml` added; full CI-identical chain green
  locally (install→build→typecheck→test→depcruise→schema-drift); README badge
  swapped to live CI badge (D7 fixed). ROADMAP T6 → `[x]`. Milestone flip
  deferred to PM-S01-2 per exit-gate wording ("CI green on main").

- 2026-07-13 — Sprint opened. Verified by direct test run: 17/17 tests green on
  `feat/aeos-p1-m1-contracts` @ `6c8858e` (T1–T5 confirmed done in code, not
  just docs). PM system bootstrapped this same day.
- 2026-07-13 — Concurrent session activity absorbed into the system: T6
  started (`13035e9`, step 1 of 5 — but `pnpm depcruise` left red, logged as
  drift D6); stylized public README added (`b6f37a1`..`03a283e`, R6-checked:
  links into PM system, no duplication; static test badge logged as D7).
  ROADMAP T6 flipped to `[~]`; T6 card updated with mid-task resume context.
- 2026-07-13 — ROADMAP extended end-to-end per Kabeer's directive: P2 (v0.2,
  8 milestones/25 tasks), P3 (v0.3, 5/11), P4 (v0.4, 4/10), new P5 (v1.0
  public open-source release, 5/18), post-v1 backlog B1–B4. Drift D5 resolved
  (multi-user auth → post-v1, spec wins). Sprint scope unchanged.
