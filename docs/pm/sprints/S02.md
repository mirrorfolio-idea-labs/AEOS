# Sprint 02 — Execute M2 (kernel)

- **Opened:** 2026-07-13
- **Sprint goal:** M2 exit gate passes (crash-safe atomic writes ×100 + reindex
  rebuild-equivalence green in CI) and the M3 plan exists.
- **Milestone:** [P1.M2](../../ROADMAP.md#m2--kernel-state-store-registry-event-bus--) ·
  Plan: [M2 kernel plan](../../superpowers/plans/2026-07-13-aeos-p1-m2-kernel.md)
- **Branch in flight:** `feat/aeos-p1-m2-kernel` (from `main`)
- **Status:** `[~]`

## Scope

### Build tasks (status owned by ROADMAP)

`AEOS-P1.M2.T1`–`T5` — see [ROADMAP M2](../../ROADMAP.md) for accepts and the
[M2 plan](../../superpowers/plans/2026-07-13-aeos-p1-m2-kernel.md) for
step-level detail. Execution runs the SDD loop (fresh implementer subagent +
task-scoped review per task); delegation cards are created just-in-time in
`../tasks/` per task entering execution and archived on completion.

### PM tasks

#### PM-S02-1 `[x]` Close-out bookkeeping at M2 exit
- **Objective:** flip M2 `[x]`, run drift scan (R5), archive task cards,
  regenerate BOARD + TRACEABILITY, close this sprint with a retrospective.
- **Depends on:** AEOS-P1.M2.T5.

#### PM-S02-2 `[ ]` Author M3 plan

- **Objective:** write `docs/superpowers/plans/<date>-aeos-p1-m3-runner.md`
  covering M3.T1–T4 to the same cold-start standard.
- **Context:** ROADMAP M3 context brief + spec §4, §10.
- **Gate:** may only start after M2 exit gate passes (ROADMAP rule).
- **Depends on:** PM-S02-1.

## Log (append-only)

- 2026-07-13 — **AEOS-P1.M2.T1 completed** (`7119e90`): AEOS_HOME layout,
  atomic writes (tmp+fsync+rename, ×100 crash-sim), YAML codecs with Zod
  validation. Verified by direct run: 44/44 tests, typecheck, depcruise all
  green. Card archived. T2 (SQLite derived index + reindex) entering execution.
- 2026-07-13 — **AEOS-P1.M2.T2 completed**: better-sqlite3 WAL index
  (`agents`/`sessions` tables), upserts, `reindex()` full rebuild in one
  transaction with corrupt-file reporting; exit-gate rebuild-equivalence test
  green (delete `index.db` → reindex → identical queries); schema-version
  mismatch drops+rebuilds. 30/30 kernel tests, depcruise 73 modules clean.
- 2026-07-13 — **AEOS-P1.M2.T3 completed**: workspace/agent CRUD over files
  (atomic + schema-validated), agent dir git-versioned with local identity and
  one commit per mutation; `.gitignore` keeps `sessions/ worktrees/ harness/`
  out of agent history; index upserted in the same call; reindex-equivalence
  tied to T2. **Layout decision:** added `workspaces/<ws>/workspace.yaml`
  (spec §7 tree omitted where workspace name lives — files-are-truth needs
  it; promote to the spec at M2 exit doc pass). 37/37 kernel tests.
- 2026-07-14 — **AEOS-P1.M2.T4 completed**: typed pub/sub event bus
  (queue-drained — publish-during-handler keeps global order; per-subscriber
  isolation), filters (type prefix / agentId / sessionId), transcript writer
  appending session events as NDJSON via index-resolved paths (AEOS-ULID
  routing, spec §7). **Plan deviation (recorded):** taxonomy is a closed
  union, so handler failures surface via `onHandlerError` callback instead of
  the plan's synthetic `bus.error` event. 44/44 kernel tests.
- 2026-07-14 — **AEOS-P1.M2.T5 completed**: `Module`/`createKernel` lifecycle
  (ordered start, reverse stop, rollback on failed start, aggregated health);
  `apps/aeosd` composition root (home → index-db → event-bus + transcript
  modules), `aeosd reindex` subcommand, signals owned solely by `main.ts`.
  Removed obsolete `apps/.gitkeep` (broke the vitest `apps/*` glob). Real
  binary smoke-tested. Full chain green: 69/69 tests across contracts/kernel/
  aeosd, typecheck, depcruise (103 modules), schema drift.
- 2026-07-14 — **PM-S02-1 completed** (M2 exit pass): `--no-ff` merge to
  `main`, CI-identical chain green on `main`; M2 → `[x]`; `workspace.yaml`
  promoted into spec §7 layout; BOARD/TRACEABILITY/EPICS regenerated; R5
  drift scan clean. **Process note:** T2–T5 were executed inline by the same
  agent with the plan on hand, so no delegation cards were minted (governance
  requires cards only for delegated work — no drift).
