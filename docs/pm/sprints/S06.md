# Sprint 06 — P2.M2 budgets + audit

- **Opened:** 2026-08-25 (immediately after P2.M1 exit)
- **Goal:** P2.M2 exit gate — a runaway-loop simulation cannot outspend its
  cap: per-objective USD/token caps metered from `cost.usage`, hard-stop
  writes a checkpoint, notify fires, and the `resume-with-increase` path
  completes the objective after the cap is raised; plus an append-only
  audit trail covering every action class.
- **Milestone:** [P2.M2](../../ROADMAP.md#m2--budgets--audit-log-)
  · Plan: `docs/superpowers/plans/2026-08-25-aeos-p2-m2-budgets-audit.md`
- **Status:** `[x]` closed 2026-08-25 — P2.M2 exit gate passed.

## PM tasks

#### PM-S06-1 `[x]` Author the P2.M2 milestone plan just-in-time.

## Log (append-only)

- 2026-08-25 — Sprint opened at P2.M1 exit (44e3099 + close-out 2758c98).
- 2026-08-25 — P2.M2 executed T1→T3 in order (85b21c2 → 73a79ac →
  a6ed339). Exit gate: flagship runaway proof — across 5 human-driven
  restarts with an unchanged cap, cumulative spend never exceeds
  per-restart single-event granularity; raising objective.yaml's cap
  resumes to completion with attempts untouched.

## Retrospective

**What went right:** scheduler-as-enforcement-point kept checkpoints and
caps in one place; the meter itself stayed pure and trivially testable.
The budget e2e caught a REAL load-dependent race in its own waitSettled
(fixed by demanding observed terminal state — S04 lesson applied).

**What to watch:** fixture cost granularity defines trip semantics
(caps must sit below per-event spend to trip within one event); audit
.ndjson (§7) vs .ndjsonl (§11) spec naming drift logged as D9; the
memory.written emission hook awaits its first live caller (P2.M4 curator).
